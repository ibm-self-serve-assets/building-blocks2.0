{
  "_description": "Tool-call guardrail. CRITICAL: OpenAI function-calling format — NOT flat dicts. See RULE 5.",
  "_metrics_used": ["Tool Call Accuracy", "Tool Call Relevance (requires WXG_PROJECT_ID)"],
  "_score_note": "Tool Call Accuracy returns None for syntactically valid calls (treated as Pass). A non-None score indicates a problem.",
  "input_text": "What's the weather in Tokyo?",
  "tool_calls": [
    {
      "type": "function",
      "function": {
        "name": "get_weather",
        "arguments": "{\"city\": \"Tokyo\"}"
      }
    }
  ],
  "available_tools": [
    {
      "type": "function",
      "function": {
        "name": "get_weather",
        "description": "Get the current weather for a city.",
        "parameters": {
          "type": "object",
          "properties": {"city": {"type": "string", "description": "City name"}},
          "required": ["city"]
        }
      }
    }
  ],
  "metrics": ["Tool Call Accuracy", "Tool Call Relevance"]
}
